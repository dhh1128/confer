# confer — known gaps

Issues discovered post-implementation that aren't yet structured into `this.i` decisions or open tensions. Each entry has enough detail that a future session can pick it up without re-deriving the analysis.

When we resolve a gap, either (a) promote it to a `this.i` decision + code change, or (b) defer it as an open `tension:` node in `this.i` with a `revisit-when:`, then remove it from this file.

---

_No open gaps._

History (resolved, kept here only as a breadcrumb to the `this.i` nodes that absorbed them):

- **G1** — MCP self-description insufficient for a memory-less agent. Resolved by `4kxp7qnj` (Notify Self-Description Policy).
- **G2** — after-fix acid test of the self-description. Ran 2026-05-29 against the full notify/ask/check_messages surface; a confer-naive subagent scored 11/11 on tool discrimination. Validated `4kxp7qnj`; resolved the rename question `3pqvn7mw` against renaming; prompted two wording refinements. Now a recurring audit in `this.i` Prompt Audit History (`vp4nm7qx`, id `sd7acidq`).
- **G3** — threaded-conversation conventions (subject handles / "Re:"). Resolved by the THREADED CHANNEL decisions in `this.i` (`tgq4n7px`, `dm5kqv7n`, `rt7nqp4m`, `nr4kpq7v`, `cg7vnq4p`, `tv4nqk7p`) and the F-A CLI-resolve closing-DM fix in `pkn7mvq4`.
